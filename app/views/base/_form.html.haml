= form_tag '/name_input', :method => 'post', :id => 'name_input_form' do
	= label_tag :name, ' '
	= text_field_tag :name, nil, {:placeholder => "#{session[:name] ? session[:name] : 'MAX'}", :class => 'name_input_field', :maxlength => 3, :size => 4}
	= submit_tag "GO"
